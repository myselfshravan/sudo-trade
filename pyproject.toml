[project]
name = "sudo-trade"
version = "0.1.0"
description = "trades need sudo privileges"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["growwapi", "pyotp", "aiohttp"]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "pytest-asyncio>=1.3.0",
]
